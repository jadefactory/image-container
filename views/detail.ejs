<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script
    src="https://code.jquery.com/jquery-3.5.1.min.js"
    integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
    crossorigin="anonymous"></script>
  <style>
    a {
      text-decoration: none;
      color: #fff;
    }
    .image-box {
      width: 500px;
      height: 500px;
      margin: 0 auto;
      text-align: center;
      margin-top: 100px;
    }
    img {
      width: 400px;
      height: 400px;
    }
    .btn-box {
      display: flex;
      justify-content: center;
      margin-top: 30px;
    }
    .btn-box button.delete,
    .btn-box button.home {
      width: 100px;
      height: 30px;
      background: #f03e3e;
      border: none;
      outline: none;
      border-radius: 10px;
      color: #fff;
      margin-right: 10px;
      cursor: pointer;
    }
    .btn-box button.delete:hover {
      background:#c92a2a;
    }
    .btn-box button.home {
      margin-right: 0;
      background:#37b24d;
    }
    .btn-box button.home:hover {
      background: #2b8a3e
    }
    
  </style>
</head>
<body>
  <div class="image-box">
    <img src="../image/<%= imageFile.url%>" alt="images">
    <div class="btn-box">
      <button class="delete" data-id="<%= imageFile._id %>">삭제하기</button>
      <button class="home"><a href="/">홈으로 이동</a></button>
    </div>
    
  </div>

  <script>
    $('.delete').click(function(e){
      if(confirm('정말 삭제하시겠습니까?')){
        let idNumber = e.target.dataset.id;
        $.ajax({
          method: 'DELETE',
          url: '/delete',
          data: {_id: idNumber}
        }).done(function(result){
          console.log('DELETE SUCCESS')
          $(location).attr('href', 'index.ejs');
        })
      } else {
        e.preventDefault();
      }
    })
  </script>
</body>
</html>